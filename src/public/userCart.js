const socket = io()

document.addEventListener("DOMContentLoaded", function () {
    const userMail = document.getElementById("emailUsuario").innerText
    const comprarBtn = document.getElementById("comprarBtn")

    document.addEventListener("click",()=>{
        
    } )

})